# Python dependencies for the NWP->LISFLOOD-FP coupling and analysis pipeline.
# Tested with Python 3.11 and the versions noted below; later minor versions
# are expected to work.
numpy>=1.24
pandas>=2.0
xarray>=2024.0
scipy>=1.11
pyproj>=3.6
rasterio>=1.3
matplotlib>=3.7
netCDF4>=1.6
